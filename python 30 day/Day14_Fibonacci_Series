# Fibonacci Series
# Generate the Fibonacci series up to n terms.
# Example:
# Input: 5
# Output: [0, 1, 1, 2, 3]

num=int(input("Enter the number:"))
a=0
b=1

for i in range(num):
    print(a,end=" ")
    a,b=b,a+b